{
  "name": "camazac-backend",
  "version": "1.0.0",
  "type": "module",
"scripts": {
  "dev": "nodemon server/index.ts",
  "build": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outfile=dist/index.js",
  "start": "node dist/index.js"
}

  
  ,
  "dependencies": {
    "@types/node": "^24.10.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
    
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
  
}
